---
// 书籍列表页
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, ''); // 移除尾部斜杠，统一处理

// 获取所有书籍并按阅读日期排序
const books = await getCollection('books');
const sortedBooks = [...books].sort((a, b) =>
  new Date(b.data.readDate).getTime() - new Date(a.data.readDate).getTime()
);

// 计算统计信息
const totalBooks = books.length;
const avgRating = totalBooks > 0
  ? (books.reduce((sum, book) => sum + book.data.rating, 0) / totalBooks).toFixed(1)
  : 0;
const favoriteCount = books.filter(b => b.data.favorite).length;
---

<Layout title="书籍清单">
  <div class="page-header">
    <h1>📚 书籍清单</h1>
    <div class="stats-summary">
      <div class="stat-item">
        <span class="stat-value">{totalBooks}</span>
        <span class="stat-name">总书籍数</span>
      </div>
      <div class="stat-item">
        <span class="stat-value">{avgRating} ⭐</span>
        <span class="stat-name">平均评分</span>
      </div>
      <div class="stat-item">
        <span class="stat-value">{favoriteCount}</span>
        <span class="stat-name">最爱书籍</span>
      </div>
    </div>
  </div>

  {sortedBooks.length === 0 ? (
    <p style="text-align: center; color: var(--color-text-secondary); margin-top: 3rem;">
      暂无书籍记录，快去添加你的第一本书吧！
    </p>
  ) : (
    <div class="grid">
      {sortedBooks.map(book => (
        <Card
          title={book.data.title}
          rating={book.data.rating}
          date={book.data.readDate}
          tags={book.data.tags}
          href={`${base}/books/${book.slug}`}
          cover={book.data.cover}
          favorite={book.data.favorite}
          meta={book.data.author}
        />
      ))}
    </div>
  )}
</Layout>

<style>
  .page-header {
    margin-bottom: 2rem;
  }

  .stats-summary {
    display: flex;
    gap: 2rem;
    flex-wrap: wrap;
    margin-top: 1.5rem;
    padding: 1.5rem;
    background-color: var(--color-bg-secondary);
    border-radius: var(--radius);
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .stat-value {
    font-size: 1.5rem;
    font-weight: bold;
    color: var(--color-primary);
  }

  .stat-name {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    margin-top: 0.25rem;
  }

  @media (max-width: 768px) {
    .stats-summary {
      gap: 1rem;
      justify-content: center;
    }
  }
</style>
