# 🚀 部署指南

## 推荐方案：部署到根域名（最简单）

### 步骤 1：创建/重命名仓库

在 GitHub 上创建一个仓库，名称为：
```
your-username.github.io
```
（把 `your-username` 替换成你的 GitHub 用户名）

### 步骤 2：修改配置

编辑 `astro.config.mjs`，确保 `base` 被注释掉（当前已经是这样）：

```javascript
export default defineConfig({
  site: 'https://your-username.github.io',
  // base 注释掉，部署到根路径
  outDir: './dist',
});
```

### 步骤 3：推送代码

```bash
# 如果是新仓库
git remote set-url origin https://github.com/your-username/your-username.github.io.git

# 推送代码
git add .
git commit -m "Initial deployment"
git push -u origin main
```

### 步骤 4：启用 GitHub Pages

1. 进入仓库的 **Settings** → **Pages**
2. Source 选择 **GitHub Actions**
3. 等待部署完成（查看 Actions 标签页）

### 步骤 5：访问网站

```
https://your-username.github.io
```

✅ **优势**：
- 所有环境路径一致，都是根路径 `/`
- 配置简单，不需要处理 base 路径
- 本地和线上行为完全一致

---

## 备选方案：部署到子路径

如果你想保留仓库名 `life-achievements`，或者想同时托管多个项目：

### 步骤 1：修改配置

编辑 `astro.config.mjs`，取消注释 `base`：

```javascript
export default defineConfig({
  site: 'https://your-username.github.io',
  base: '/life-achievements',  // 取消注释这一行
  outDir: './dist',
});
```

### 步骤 2：推送代码

```bash
git add .
git commit -m "Add base path"
git push
```

### 步骤 3：访问网站

```
https://your-username.github.io/life-achievements
```

⚠️ **注意**：
- 本地开发仍然是 `http://localhost:4321/`（根路径）
- 线上是 `https://your-username.github.io/life-achievements`（子路径）
- 所有链接会自动加上 `/life-achievements` 前缀

---

## 🎯 我的推荐

**推荐使用方案 1（根域名部署）**，因为：

1. ✅ **配置更简单** - 不需要处理 base 路径
2. ✅ **本地和线上一致** - 减少混淆
3. ✅ **URL 更简洁** - `username.github.io` 比 `username.github.io/life-achievements` 更简短
4. ✅ **易于维护** - 少一个配置项，少一个出错的可能

### 如果你有多个项目怎么办？

如果你想在 GitHub Pages 上托管多个项目：

1. **主项目**：使用 `username.github.io` 仓库，部署到根路径
2. **其他项目**：使用各自的仓库名，部署到子路径

例如：
- 主网站：`https://your-username.github.io` (这个成就展示网站)
- 其他项目：`https://your-username.github.io/project1`
- 其他项目：`https://your-username.github.io/project2`

---

## 📝 当前配置状态

当前 `astro.config.mjs` 已配置为 **根域名部署模式**：
- ✅ `base` 已注释
- ✅ 适合部署到 `username.github.io` 仓库
- ✅ 本地和线上路径都是根路径

如果你的仓库名是 `life-achievements`，需要取消注释 `base: '/life-achievements',` 这一行。

---

## 🔧 快速检查清单

部署前检查：

- [ ] 确认仓库名（`username.github.io` 或 `life-achievements`）
- [ ] 根据仓库名决定是否需要配置 `base`
- [ ] 修改 `site` 中的用户名
- [ ] 推送代码
- [ ] 在 GitHub Settings → Pages 启用 GitHub Actions
- [ ] 等待部署完成
- [ ] 访问网站验证

---

## ❓ 常见问题

### Q: 我可以随时切换部署方式吗？

A: 可以！只需要：
1. 修改 `astro.config.mjs` 中的 `base` 配置
2. 推送代码
3. 重新部署即可

### Q: 本地开发时需要修改配置吗？

A: **不需要！**
- 如果使用根域名部署（无 base），本地和线上都一样
- 如果使用子路径部署（有 base），Astro 会自动处理

### Q: 我应该选择哪种方式？

A:
- **个人主页/博客** → 根域名部署（推荐）
- **多个项目需要同时展示** → 子路径部署
- **不确定** → 先用根域名，以后可以改

---

## 🎊 开始部署吧！

选择一种方式，按照步骤操作，几分钟后你的网站就能上线了！🚀
