# 使用指南

## 📝 日常使用流程

### 1. 添加新内容

当你完成了一个游戏、看了一部电影或读了一本书，按以下步骤添加：

#### 添加游戏

```bash
# 1. 在 src/content/games/ 目录创建新的 .md 文件
# 文件名建议使用英文（会成为 URL 的一部分）
cd src/content/games
touch cyberpunk-2077.md
```

编辑文件，添加 Front Matter 和内容：

```markdown
---
title: "赛博朋克 2077"
platform: "PC"
rating: 4
completedDate: "2024-10-14"
status: "已完成"
playTime: 80
tags: [RPG, 科幻, 开放世界]
favorite: false
---

## 我的评价

经过多次更新后，游戏体验有了很大改善...
```

#### 添加电影/书籍

同样的方式，在对应目录下创建 .md 文件。

### 2. 添加封面图片（可选）

```bash
# 1. 将图片放到 public/images/ 目录
cp ~/Downloads/game-cover.jpg public/images/

# 2. 在 Markdown 文件的 Front Matter 中引用
cover: "/images/game-cover.jpg"
```

### 3. 本地预览

```bash
npm run dev
```

访问 `http://localhost:4321` 查看效果。

### 4. 提交并部署

```bash
git add .
git commit -m "添加《赛博朋克 2077》游戏记录"
git push
```

推送后，GitHub Actions 会自动构建和部署，几分钟后就能在线上看到更新。

## 🎨 自定义网站

### 修改主题色

编辑 `src/layouts/Layout.astro`，修改 CSS 变量：

```css
:root {
  --color-primary: #3b82f6;  /* 改为你喜欢的颜色 */
}
```

### 修改网站标题和描述

编辑 `src/layouts/Layout.astro` 中的 `<title>` 标签和 `src/components/Header.astro` 中的 logo 文本。

### 添加新的分类（高级）

如果想添加新的内容类型（比如音乐、播客等）：

1. 在 `src/content/config.ts` 添加新的 collection schema
2. 创建 `src/content/music/` 目录
3. 创建 `src/pages/music.astro` 列表页
4. 创建 `src/pages/music/[slug].astro` 详情页
5. 在 `src/components/Header.astro` 添加导航链接

## 📊 数据管理

### Front Matter 字段说明

#### 游戏 (games)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | ✅ | 游戏名称 |
| platform | string | ✅ | 游戏平台 |
| rating | number (1-5) | ✅ | 评分 |
| completedDate | string (YYYY-MM-DD) | ✅ | 完成日期 |
| status | enum | ✅ | 状态：已完成/进行中/已放弃 |
| tags | array | ✅ | 标签数组 |
| playTime | number | ❌ | 游戏时长（小时） |
| cover | string | ❌ | 封面图片路径 |
| favorite | boolean | ❌ | 是否最爱 |

#### 电影 (movies)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | ✅ | 电影名称 |
| director | string | ✅ | 导演 |
| year | number | ✅ | 上映年份 |
| rating | number (1-5) | ✅ | 评分 |
| watchDate | string (YYYY-MM-DD) | ✅ | 观看日期 |
| tags | array | ✅ | 标签数组 |
| cover | string | ❌ | 海报路径 |
| favorite | boolean | ❌ | 是否最爱 |

#### 书籍 (books)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | ✅ | 书名 |
| author | string | ✅ | 作者 |
| rating | number (1-5) | ✅ | 评分 |
| readDate | string (YYYY-MM-DD) | ✅ | 阅读完成日期 |
| tags | array | ✅ | 标签数组 |
| cover | string | ❌ | 封面路径 |
| favorite | boolean | ❌ | 是否最爱 |

### 日期格式

所有日期必须使用 `YYYY-MM-DD` 格式，例如：

- ✅ `2024-10-14`
- ✅ `2023-01-05`
- ❌ `2024/10/14`
- ❌ `14-10-2024`
- ❌ `Oct 14, 2024`

### 标签使用建议

**游戏标签示例：**
- 类型：RPG, 动作, 冒险, 策略, 模拟, 射击
- 风格：像素, 赛博朋克, 奇幻, 科幻, 恐怖
- 特点：开放世界, 线性流程, Roguelike, 多人联机

**电影标签示例：**
- 类型：剧情, 喜剧, 动作, 科幻, 恐怖, 悬疑, 爱情
- 特点：黑色幽默, 反转, 烧脑, 治愈, 励志

**书籍标签示例：**
- 类型：小说, 非虚构, 传记, 历史, 科普, 心理学
- 风格：科幻, 推理, 奇幻, 现实主义

## 🔧 故障排查

### 构建失败

如果构建失败，检查：

1. **Front Matter 格式错误**
   - 确保所有必填字段都填写了
   - 日期格式是否正确
   - tags 是否使用数组格式 `[标签1, 标签2]`
   - rating 是否在 1-5 范围内

2. **TypeScript 类型错误**
   ```bash
   npm run build
   ```
   查看详细错误信息并修复。

3. **图片路径错误**
   - 图片必须放在 `public/images/` 目录
   - 路径以 `/images/` 开头（不是 `public/images/`）

### GitHub Actions 部署失败

1. 确认 GitHub Pages 已启用
2. 确认 Source 选择了 "GitHub Actions"
3. 检查 `.github/workflows/deploy.yml` 文件是否存在
4. 查看 Actions 标签页中的错误日志

### 本地开发端口被占用

```bash
# 使用不同的端口
npm run dev -- --port 3000
```

## 💡 使用技巧

### 批量添加内容

如果要添加多个内容，可以：

1. 先创建所有 .md 文件（可以复制模板）
2. 批量编辑 Front Matter
3. 一次性提交

### 数据备份

由于所有数据都是 Markdown 文件，可以：

- 定期 push 到 GitHub（自动备份）
- 使用 Git tag 标记重要版本
- 导出整个 `src/content/` 目录作为备份

### Markdown 写作技巧

- 使用 `##` 和 `###` 组织内容结构
- 使用列表记录要点
- 可以使用图片、引用等 Markdown 语法
- 内容会自动应用网站样式

## 📈 未来增强建议

可以考虑添加的功能：

- [ ] 按标签筛选内容
- [ ] 搜索功能
- [ ] RSS 订阅
- [ ] 评论系统
- [ ] 数据可视化图表
- [ ] 导出数据为 JSON/CSV
- [ ] 暗色主题切换

## 🆘 获取帮助

- 查看 [Astro 官方文档](https://docs.astro.build/)
- 查看 [Astro Content Collections 指南](https://docs.astro.build/en/guides/content-collections/)
- 提交 Issue 到项目仓库

祝你使用愉快！ 🎉
